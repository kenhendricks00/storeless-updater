name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+-*"

permissions:
  contents: write       # create the GitHub Release
  id-token: write       # OIDC for Sigstore signing
  attestations: write   # publish the build-provenance attestation

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Build (release)
        run: cargo build --locked --release

      - name: Stage artifact
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item target/release/binaryferry.exe dist/binaryferry.exe
          $hash = (Get-FileHash dist/binaryferry.exe -Algorithm SHA256).Hash.ToLower()
          "$hash  binaryferry.exe" | Out-File -FilePath dist/binaryferry.exe.sha256 -Encoding ASCII -NoNewline
          Write-Host "SHA-256: $hash"

      - name: Generate build provenance attestation
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: dist/binaryferry.exe

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          body_path: RELEASE_NOTES.md
          files: |
            dist/binaryferry.exe
            dist/binaryferry.exe.sha256
